import { Typography } from "@mui/material"

export const Contact=()=>{
    return(
        <div>
            <Typography variant="h5">For any Queries Contact admin@ymail.com</Typography>
        </div>
    )
}